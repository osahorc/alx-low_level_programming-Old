#include <unistd.h>

/**
 * print_alphabet - writes the character c to stdout
 * description:- The character to print
 * Return: On successnull
 * On error, null is returned, and errno is set appropriately.
 */
void print_alphabet(void)
{
	return;
}
