#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - This function counts the lenght of the string
 * @s: the string to be calculated
 *
 * Return: void
 */

int _strlen(char *s)
{
	strlen(s);
return:0
}
